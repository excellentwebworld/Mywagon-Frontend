# Copy to .env for local overrides, or use mode-specific files:
#   .env.development  — local dev (npm run dev)
#   .env.staging        — staging build (npm run build:staging)
#   .env.production     — production build (npm run build)

# Shipper panel API (Sanctum bearer auth)
# Local:  /api/shipper/v1  (proxied via Vite to VITE_LARAVEL_URL)
# Staging: https://staging.myvagon.com/api/shipper/v1
VITE_API_BASE_URL=/api/shipper/v1

# Laravel backend URL (Vite dev proxy target only)
VITE_LARAVEL_URL=http://localhost:8000

# Socket.IO server (must include port — same as Laravel SOCKET_URL)
VITE_SOCKET_URL=http://localhost:3000

# React router base path
VITE_BASE_PATH=/

# Legacy axios client (optional — loginAPI / forgotPasswordAPI)
VITE_API_URL=
VITE_API_VERSION=v1

# Google Maps (Places autocomplete + detail map embed)
VITE_GOOGLE_MAPS_KEY=

# Search Trucks: set true to use local MOCK_TRUCKS / MOCK_PENDING (offline demo)
# Default (unset/false) uses live /api/shipper/v1/availabilities
VITE_USE_SEARCH_TRUCKS_MOCK=

# Firebase Cloud Messaging (FCM Web Push)
# Optional in dev: leaving unset gracefully disables push notifications without errors.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_VAPID_KEY=

